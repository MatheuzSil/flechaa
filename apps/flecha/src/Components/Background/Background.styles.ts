import styled from 'styled-components';

export const LoginBackgroundContainer = styled.div<any>`
  width: 100%;
  height: 100vh;
  background: #38ACFF;
  background: radial-gradient(circle,rgba(56, 172, 255, 1) 0%, rgba(160, 51, 255, 1) 100%);
  z-index: 0;
  position: absolute;
`;
