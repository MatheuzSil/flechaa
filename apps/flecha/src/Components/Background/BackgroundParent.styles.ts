import styled from 'styled-components';

export const ParentLoginBackgroundContainer = styled.div<any>`
  width: 100%;
  height: 100vh;
  background: #38ACFF;
  background: linear-gradient(90deg,rgba(224, 195, 252, 1) 0%, rgba(255, 255, 255, 1) 100%);
  z-index: 0;
  position: absolute;
`;
